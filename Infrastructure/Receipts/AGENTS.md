# AGENTS.md — Infrastructure/Receipts/

Implementação do `IReceiptsService`.

Arquivos:
- ReceiptService.cs

Responsabilidade:
- CRUD de Receipts e verificação de `OrderId`/`CustomerId`.
- Persistência via `AppDbContext`.
